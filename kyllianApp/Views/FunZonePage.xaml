<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="kyllianApp.Views.FunZonePage"
             Title="FunZone">

    <ScrollView>
        <StackLayout Padding="20" Spacing="30">

            <!-- Citation -->
            <Label Text="Citation aléatoire" FontAttributes="Bold" FontSize="20" />
            <Label x:Name="QuoteLabel" FontSize="16" TextColor="DarkGreen" />
            <Button Text="Nouvelle citation" Clicked="OnGetQuoteClicked" />

            <!-- Roulette de personnage -->
            <Label Text="Personnage au hasard" FontAttributes="Bold" FontSize="20" />
            <Image x:Name="RandomCharacterImage" HeightRequest="200" Aspect="AspectFill" />
            <Label x:Name="RandomCharacterName" FontSize="18" FontAttributes="Bold" />
            <Button Text="Tirer au sort un personnage" Clicked="OnPickRandomCharacterClicked" />

        </StackLayout>
    </ScrollView>
</ContentPage>
